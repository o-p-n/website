<!DOCTYPE html>
<html>
  <head>
    <title>{%if title %}{{title}} :: {% endif %}{{ site.title }}</title>
    <meta charset="utf-8"/>
    {% set style %}
      {% include "../css/typography.css" %}
    {% endset %}
    <style>
      {{ style | safe }}
    </style>
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>